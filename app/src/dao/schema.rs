table! {
    CUSTOMER (ID) {
        ID -> Integer,
        DOCUMENT -> Text,
        NAME -> Text,
        SECOND_NAME -> Nullable<Text>,
        PERSON_TYPE -> Text,
        DEVICE_ID -> Nullable<Text>,
    }
}
